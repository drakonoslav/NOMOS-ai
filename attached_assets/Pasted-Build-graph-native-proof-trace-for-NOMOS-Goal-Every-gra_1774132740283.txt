Build graph-native proof trace for NOMOS.

Goal:
Every graph-executed constraint must produce a proof trace that references:
- selected nodes
- filtered nodes
- excluded nodes
- applied window
- aggregate result
- final comparison result

This proof trace must be deterministic and tied directly to graph execution.

Create:
- src/graph/graph_proof_types.ts
- src/graph/graph_proof_trace.ts
- update graph_constraint_executor.ts
- src/ui/components/evaluation/GraphProofTracePanel.tsx
- src/tests/graph_proof_trace_test.ts

Requirements:

1. Add canonical proof types.

interface GraphProofStep {
  stepNumber: number;
  label: string;
  description: string;

  selectedNodeIds?: string[];
  excludedNodeIds?: string[];

  data?: Record<string, unknown>;
}

interface GraphConstraintProofTrace {
  constraintId: string;
  label: string;

  candidateId?: string | null;

  steps: GraphProofStep[];

  finalObservedValue: number;
  operator: string;
  threshold: number;
  passed: boolean;
}

2. Proof trace must include these stages where applicable:
- candidate entity selection
- tag/class filtering
- window restriction
- excluded nodes after filtering/windowing
- aggregation result
- threshold comparison

3. Example proof lines for carb timing:
- "Selected all entities belonging to candidate D."
- "Filtered entities tagged fast + carb."
- "Restricted entities to within 90 minutes before lifting."
- "Excluded oats node because it is tagged slow."
- "Aggregated qualifying grams = 60."
- "Compared 60 >= 60 -> pass."

Second constraint:
- "Filtered entities tagged slow + carb."
- "Restricted entities to within 60 minutes before lifting."
- "Aggregated qualifying grams = 30."
- "Compared 30 <= 20 -> fail."

4. Update graph_constraint_executor.ts so every execution returns:
- GraphConstraintExecutionResult
- GraphConstraintProofTrace

5. Proof trace must preserve ids of:
- selected nodes
- excluded nodes
- anchor/window nodes if available

6. UI:
Create GraphProofTracePanel showing:
- ordered proof steps
- final observed value
- threshold comparison
- pass/fail result

7. Keep everything deterministic.
8. Do not use LLM generation.
9. Proof trace must reflect actual graph execution, not post hoc narrative.

Success condition:
Every graph-executed constraint in NOMOS can produce a node-aware proof trace that makes pass/fail decisions fully graph-auditable.